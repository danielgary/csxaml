component Element GreetingCard(string Title) {
    render <Border AutomationProperties.Name="Greeting Card">
        <TextBlock
            AutomationProperties.Name="Greeting Title"
            Text={Title} />
    </Border>;
}
